name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *" # 10분마다
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: |
          curl -s https://react-metro.onrender.com/health > /dev/null
